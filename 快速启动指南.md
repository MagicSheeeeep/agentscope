# 🚀 AgentScope 快速启动指南

## 📋 项目概述

AgentScope 是一个强大的多智能体编程框架，支持与多种大语言模型进行交互。本指南将帮助您快速上手使用。

## ✅ 环境确认

### 1. 检查安装状态
```bash
python3 -c "import agentscope; print('AgentScope 版本:', agentscope.__version__)"
```

### 2. 如果未安装
```bash
pip3 install agentscope --user
```

## 🎯 两种使用方式

### 方式一：命令行交互 🖥️

**适用场景**：日常对话、快速测试、简单交互

```bash
python3 start_agentscope.py
```

**特点**：
- ✅ 启动快速，无需额外配置
- ✅ 纯命令行界面，简洁高效
- ✅ 支持连续对话
- ✅ 自动统计对话轮数

**使用示例**：
```
🚀 AgentScope 命令行对话
==================================================
✅ AgentScope 初始化成功！
💬 开始对话（输入 'exit'、'quit' 或 '退出' 结束对话）
🤖 AI助手: 你好！我是通义千问，有什么可以帮助您的吗？
--------------------------------------------------
User Input: 你好，请介绍一下自己
🤖 AI助手: 你好！我是通义千问，由阿里巴巴开发的大语言模型...
```

### 方式二：可视化界面 📊

**适用场景**：调试分析、演示展示、流程监控

**步骤1：启动可视化服务**
```bash
as_studio
```

**步骤2：运行连接脚本**
```bash
python3 chat_with_studio.py
```

**特点**：
- ✅ 实时可视化对话流程
- ✅ 监控AI思考过程
- ✅ 自动检测Studio状态
- ✅ 包含演示模式

**可视化界面功能**：
- 📈 对话流程图
- 🔄 消息传递可视化
- 📊 性能监控
- 🎯 智能体状态追踪

## 🔧 配置说明

### API密钥配置

两个脚本都已预配置阿里云DashScope API密钥：
```python
MODEL_CONFIG = [
    {
        "config_name": "qwen_turbo",
        "model_type": "dashscope_chat",
        "model_name": "qwen-turbo",
        "api_key": "sk-cc1605a341c4450489bd71ffc28238c0",
        # ...
    }
]
```

### 如需更换API密钥

1. **获取新的API密钥**：
   - 阿里云DashScope: https://dashscope.console.aliyun.com/
   - OpenAI: https://platform.openai.com/
   - 智谱AI: https://open.bigmodel.cn/

2. **修改配置**：
   编辑脚本中的 `api_key` 字段

## 📝 使用示例

### 命令行对话示例

```bash
# 启动命令行对话
python3 start_agentscope.py

# 示例对话
User Input: 什么是机器学习？
🤖 AI助手: 机器学习是人工智能的一个分支...

User Input: 能举个例子吗？
🤖 AI助手: 当然可以！比如推荐系统...

User Input: exit
👋 感谢使用AgentScope，再见！
📊 本次对话共进行了 2 轮
```

### Studio可视化示例

```bash
# 1. 启动Studio
as_studio

# 2. 运行可视化测试
python3 chat_with_studio.py

# 输出示例
🎯 AgentScope Studio 可视化测试工具
============================================================
🔍 检查AgentScope Studio状态...
✅ AgentScope Studio 正在运行 (http://localhost:3000)
🔧 初始化AgentScope并连接到Studio...
✅ AgentScope 初始化成功！
📊 已连接到Studio，可视化界面将显示对话过程
📱 Studio界面地址: http://localhost:3000
```

## 🛠️ 故障排除

### 常见问题及解决方案

#### 1. 模块导入错误
```
ModuleNotFoundError: No module named 'agentscope'
```
**解决方案**：
```bash
pip3 install agentscope --user
```

#### 2. API密钥错误
```
InvalidApiKey: Invalid API-key provided
```
**解决方案**：
- 检查API密钥是否正确
- 确认账户余额充足
- 验证API密钥权限

#### 3. Studio连接失败
```
❌ 无法连接到AgentScope Studio
```
**解决方案**：
```bash
# 安装并启动Studio
npm install -g @agentscope/studio
as_studio
```

#### 4. 网络连接问题
**解决方案**：
- 检查网络连接
- 确认防火墙设置
- 尝试使用代理

### 调试模式

如需查看详细日志：
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

## 🎮 高级功能

### 1. 多智能体对话

可以扩展脚本支持多个AI助手协作：
```python
# 创建多个智能体
alice = DialogAgent(name="Alice", ...)
bob = DialogAgent(name="Bob", ...)

# 多智能体对话
from agentscope import msghub
with msghub(participants=[alice, bob]) as hub:
    # 智能体协作对话
```

### 2. 工具调用

为AI助手添加工具能力：
```python
from agentscope.service import ServiceToolkit, execute_python_code

toolkit = ServiceToolkit()
toolkit.add(execute_python_code)

agent = ReActAgent(
    name="编程助手",
    service_toolkit=toolkit,
    # ...
)
```

### 3. 自定义提示词

修改系统提示词来定制AI行为：
```python
dialog_agent = DialogAgent(
    name="专业助手",
    sys_prompt="你是一个专业的技术顾问，请提供准确的技术建议。"
)
```

## 📚 更多资源

- **官方文档**: https://doc.agentscope.io/
- **GitHub仓库**: https://github.com/modelscope/agentscope
- **示例代码**: `./examples/` 目录
- **API文档**: https://doc.agentscope.io/api/

## 🎉 快速开始

### 新手推荐流程

1. **第一次使用**：
   ```bash
   python3 start_agentscope.py
   ```
   体验基础对话功能

2. **了解可视化**：
   ```bash
   as_studio                    # 启动可视化界面
   python3 chat_with_studio.py  # 连接测试
   ```
   在浏览器中查看 http://localhost:3000

3. **探索示例**：
   ```bash
   ls examples/                 # 查看官方示例
   python3 examples/conversation_basic/conversation.py
   ```

### 日常使用建议

- **快速对话**: 使用 `start_agentscope.py`
- **调试分析**: 使用 `chat_with_studio.py`
- **学习研究**: 查看 `examples/` 目录

## 🔄 版本信息

- **AgentScope版本**: 0.1.4
- **Python要求**: 3.9+
- **支持模型**: 17+ 种不同LLM API

## 💡 使用技巧

1. **提高响应速度**: 使用命令行模式
2. **调试问题**: 使用Studio可视化模式
3. **批量测试**: 修改脚本添加自动化对话
4. **自定义功能**: 参考官方示例进行扩展

---

🎊 **现在您可以开始使用AgentScope了！**

选择适合的启动方式，开始您的AI对话之旅！ 