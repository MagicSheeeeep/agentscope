# 🚀 AgentScope 快速启动指南

## 📋 项目概述

AgentScope 是一个强大的多智能体编程框架，支持与多种大语言模型进行交互。本指南将帮助您快速上手使用。

## ✅ 环境确认

### 1. 检查安装状态
```bash
python3 -c "import agentscope; print('AgentScope 版本:', agentscope.__version__)"
```

### 2. 如果未安装
```bash
pip3 install agentscope --user
```

## 🎯 两种使用方式

### 方式一：命令行交互 🖥️

**适用场景**：日常对话、快速测试、简单交互

```bash
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/simple_chat.py
```

**特点**：
- ✅ 启动快速，无需额外配置
- ✅ 纯命令行界面，简洁高效
- ✅ 支持连续对话
- ✅ 自动统计对话轮数
- ✅ 不依赖Studio，可独立运行

**使用示例**：
```
🚀 AgentScope 纯命令行对话
==================================================
💡 此版本不依赖Studio，适合快速使用
✅ AgentScope 初始化成功！
💬 开始对话（输入 'exit'、'quit' 或 '退出' 结束对话）
🤖 AI助手: 你好！我是通义千问，有什么可以帮助您的吗？
--------------------------------------------------
👤 您: 你好，请介绍一下自己
🤖 AI助手: 你好！我是通义千问，由阿里巴巴开发的大语言模型...
```

### 方式二：可视化界面 📊

**适用场景**：调试分析、演示展示、流程监控

**步骤1：启动可视化服务**
```bash
as_studio
```

**步骤2：运行连接脚本**
```bash
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/chat_with_studio.py
```

**特点**：
- ✅ 实时可视化对话流程
- ✅ 监控AI思考过程
- ✅ 自动检测Studio状态
- ✅ 包含演示模式
- ✅ 正确的Dashboard URL格式

**可视化界面功能**：
- 📈 对话流程图
- 🔄 消息传递可视化
- 📊 性能监控
- 🎯 智能体状态追踪

## 🔧 配置说明

### 配置方式选择

**方式1：使用配置文件（推荐）**
```bash
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/config_file_demo.py
```
此脚本演示如何使用 `cursor/configs/model_configs.json` 配置文件。

**方式2：硬编码配置（当前脚本使用）**
脚本中直接定义配置：
```python
MODEL_CONFIG = [
    {
        "config_name": "qwen_turbo",
        "model_type": "dashscope_chat",
        "model_name": "qwen-turbo",
        "api_key": "your_api_key_here",
        # ...
    }
]
```

### API密钥配置

**查看可用配置**：
配置文件 `cursor/configs/model_configs.json` 包含多种模型配置：
- 阿里云DashScope (qwen-max, qwen-turbo, qwen-plus)
- OpenAI (gpt-4, gpt-3.5-turbo)
- 本地模型 (ollama)

### 如需更换API密钥

1. **获取新的API密钥**：
   - 阿里云DashScope: https://dashscope.console.aliyun.com/
   - OpenAI: https://platform.openai.com/
   - 智谱AI: https://open.bigmodel.cn/

2. **修改配置**：
   - 编辑 `cursor/configs/model_configs.json` 文件
   - 或修改脚本中的 `api_key` 字段

## 📂 文件结构说明

```
cursor/
├── configs/
│   ├── model_configs.json      # 模型配置文件
│   └── .cursorindexingignore
├── docs/
│   ├── 快速启动指南.md         # 本文档
│   ├── README_MultiModel.md
│   └── README_SameModel.md
├── scripts/                    # 主要脚本目录
│   ├── simple_chat.py          # 纯命令行对话（推荐，无需Studio）
│   ├── chat_with_studio.py     # Studio可视化测试
│   ├── start_agentscope_fixed.py # 命令行对话（需要Studio）
│   ├── config_file_demo.py     # 配置文件演示
│   ├── simple_test.py          # 简单测试
│   ├── final_solution.py       # 最终解决方案
│   └── ...                     # 其他示例脚本
└── tests/
    ├── test_interactive.py
    ├── test_start_script.py
    └── ...
```

## 📝 使用示例

### 命令行对话示例

```bash
# 启动命令行对话
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/simple_chat.py

# 示例对话
👤 您: 什么是机器学习？
🤖 AI助手: 机器学习是人工智能的一个分支...

👤 您: 能举个例子吗？
🤖 AI助手: 当然可以！比如推荐系统...

👤 您: exit
👋 感谢使用AgentScope，再见！
📊 本次对话共进行了 2 轮
🎯 查看完整对话记录: http://localhost:3000/dashboard?run_id=run_xxx
```

### Studio可视化示例

```bash
# 1. 启动Studio
as_studio

# 2. 运行可视化测试
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/chat_with_studio.py

# 输出示例
🚀 AgentScope Studio 可视化测试
============================================================
🔍 检查AgentScope Studio状态...
✅ AgentScope Studio 正在运行 (http://localhost:3000)
🔧 初始化AgentScope并连接到Studio...
✅ AgentScope 初始化成功！
📊 已连接到Studio，可视化界面将显示对话过程

🎯 重要：请访问以下正确的URL查看可视化界面：
📱 方式1 - 直接访问: http://localhost:3000/dashboard?run_id=run_xxx
📱 方式2 - Dashboard: http://localhost:3000/dashboard
⚠️  注意：使用 /dashboard 路径，避免重定向问题
```

### 配置文件演示

```bash
# 查看配置加载方式对比
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/config_file_demo.py

# 输出示例
🚀 AgentScope 配置加载方式演示
============================================================
🔧 方式1：硬编码配置
🔧 方式2：从配置文件加载
🔧 方式3：手动加载JSON并传入
📚 AgentScope 配置加载机制详解
```

## 🛠️ 故障排除

### 常见问题及解决方案

#### 1. 模块导入错误
```
ModuleNotFoundError: No module named 'agentscope'
```
**解决方案**：
```bash
pip3 install agentscope --user
```

#### 2. API密钥错误
```
InvalidApiKey: Invalid API-key provided
```
**解决方案**：
- 检查 `cursor/configs/model_configs.json` 中的API密钥
- 确认账户余额充足
- 验证API密钥权限

#### 3. Studio连接失败
```
❌ 无法连接到AgentScope Studio
```
**解决方案**：
```bash
# 安装并启动Studio
npm install -g @agentscope/studio
as_studio
```

#### 4. 脚本路径错误
```
FileNotFoundError: No such file or directory
```
**解决方案**：
- 确保使用正确的脚本路径：`cursor/scripts/xxx.py`
- 确认当前工作目录在项目根目录

#### 5. 网络连接问题
**解决方案**：
- 检查网络连接
- 确认防火墙设置
- 尝试使用代理

### 调试模式

如需查看详细日志：
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

## 🎮 高级功能

### 1. 多智能体对话

查看多智能体示例：
```bash
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/same_model_multi_agents.py
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/multi_model_orchestration.py
```

扩展脚本支持多个AI助手协作：
```python
# 创建多个智能体
alice = DialogAgent(name="Alice", ...)
bob = DialogAgent(name="Bob", ...)

# 多智能体对话
from agentscope import msghub
with msghub(participants=[alice, bob]) as hub:
    # 智能体协作对话
```

### 2. 工具调用

为AI助手添加工具能力：
```python
from agentscope.service import ServiceToolkit, execute_python_code

toolkit = ServiceToolkit()
toolkit.add(execute_python_code)

agent = ReActAgent(
    name="编程助手",
    service_toolkit=toolkit,
    # ...
)
```

### 3. 自定义提示词

修改系统提示词来定制AI行为：
```python
dialog_agent = DialogAgent(
    name="专业助手",
    sys_prompt="你是一个专业的技术顾问，请提供准确的技术建议。"
)
```

## 🧪 测试和示例脚本

### 基础测试
```bash
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/simple_test.py           # 简单对话测试
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/tests/test_interactive.py        # 交互测试
```

### 高级示例
```bash
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/simple_same_model_example.py      # 同模型多配置
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/simple_orchestration_example.py  # 简单编排
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/run_same_model_demo.py           # 演示运行
```

### 实用工具
```bash
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/get_correct_studio_url.py  # 获取正确的Studio URL
python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/realtime_test.py           # 实时测试
```

## 📚 更多资源

- **官方文档**: https://doc.agentscope.io/
- **GitHub仓库**: https://github.com/modelscope/agentscope
- **项目示例**: `./examples/` 目录
- **本地示例**: `./cursor/scripts/` 目录
- **API文档**: https://doc.agentscope.io/api/

## 🎉 快速开始

### 新手推荐流程

1. **第一次使用**：
   ```bash
   python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/simple_chat.py
   ```
   体验基础对话功能（不依赖Studio）

2. **了解可视化**：
   ```bash
   as_studio                                    # 启动可视化界面
   python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/chat_with_studio.py  # 连接测试
   ```
   在浏览器中查看 http://localhost:3000/dashboard

3. **学习配置**：
   ```bash
   python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/config_file_demo.py  # 配置方式演示
   ```

4. **探索示例**：
   ```bash
   ls /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/                           # 查看本地示例
   ls /Users/fangyuan/IdeaProjects/agentscope/examples/                                 # 查看官方示例
   python3 /Users/fangyuan/IdeaProjects/agentscope/examples/conversation_basic/conversation.py
   ```

### 日常使用建议

- **快速对话**: 使用 `python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/simple_chat.py`
- **调试分析**: 使用 `python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/chat_with_studio.py`
- **学习研究**: 查看 `/Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/` 和 `/Users/fangyuan/IdeaProjects/agentscope/examples/` 目录
- **配置管理**: 编辑 `/Users/fangyuan/IdeaProjects/agentscope/cursor/configs/model_configs.json`

## 🔄 版本信息

- **AgentScope版本**: 0.1.4+
- **Python要求**: 3.9+
- **支持模型**: 17+ 种不同LLM API
- **项目结构**: 已重新组织，脚本位于 `cursor/` 目录下

## 💡 使用技巧

1. **提高响应速度**: 使用命令行模式
2. **调试问题**: 使用Studio可视化模式
3. **批量测试**: 修改脚本添加自动化对话
4. **自定义功能**: 参考 `cursor/scripts/` 示例进行扩展
5. **配置管理**: 使用 `cursor/configs/model_configs.json` 统一管理配置
6. **获取正确URL**: 运行 `python3 /Users/fangyuan/IdeaProjects/agentscope/cursor/scripts/get_correct_studio_url.py`

---

🎊 **现在您可以开始使用AgentScope了！**

选择适合的启动方式，开始您的AI对话之旅！ 