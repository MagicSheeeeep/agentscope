# AgentScope Workstation 替代方案指南

## ❌ 重要说明

**Workstation拖拽界面在当前版本中不可用！**

您的AgentScope版本是 `0.1.5dev`，根据官方文档：
> "从v0.1.5版本开始，现有的workstation代码会从主库中剥离，该功能将处于不可用状态。"

虽然拖拽式的Workstation界面已被移除，但您仍然可以通过以下方式使用AgentScope的低代码功能。

## 🛠️ 替代方案

### 方案1：使用Gradio可视化界面

AgentScope支持通过Gradio创建web界面：

```bash
# 启动Gradio界面
as_gradio gradio_app.py
```

**特点：**
- ✅ 提供web界面交互
- ✅ 支持多模态内容
- ✅ 实时对话体验
- ❌ 不支持拖拽式开发

### 方案2：直接使用JSON配置文件

您可以手动编写JSON配置文件，然后使用命令行工具：

```bash
# 运行JSON配置
as_gradio simple_config.json

# 将JSON编译为Python代码
as_workflow simple_config.json --compile my_app.py
```

**特点：**
- ✅ 配置灵活
- ✅ 可以导出Python代码
- ❌ 需要手动编写JSON
- ❌ 没有可视化编辑器

### 方案3：直接编写Python代码

使用AgentScope的Python API直接开发：

```python
import agentscope
from agentscope.agents import DialogAgent, UserAgent

# 初始化和配置
agentscope.init(model_configs=[...])

# 创建代理
agent = DialogAgent(name="助手", model_config_name="my_model")
user = UserAgent()

# 构建对话流程
# ... 您的业务逻辑
```

**特点：**
- ✅ 最大灵活性
- ✅ 完全可控
- ✅ 易于集成
- ❌ 需要编程经验

## 📝 使用示例

### 1. 运行Gradio应用

```bash
# 确保设置了API密钥后运行
as_gradio gradio_app.py
```

### 2. 使用JSON配置

编辑 `simple_config.json` 文件：
- 将 `"api_key": "your_api_key_here"` 替换为您的实际API密钥
- 运行：`as_gradio simple_config.json`

### 3. 编译为Python代码

```bash
as_workflow simple_config.json --compile my_generated_app.py
```

然后您可以进一步编辑生成的Python代码。

## 🔧 JSON配置格式说明

JSON配置文件包含以下节点类型：

### 模型节点
```json
{
  "name": "dashscope_chat",
  "data": {
    "args": {
      "config_name": "qwen",
      "model_name": "qwen-turbo", 
      "api_key": "your_api_key",
      "model_type": "dashscope_chat"
    }
  }
}
```

### 代理节点
```json
{
  "name": "DialogAgent",
  "data": {
    "args": {
      "name": "AI助手",
      "sys_prompt": "你是一个友好的AI助手",
      "model_config_name": "qwen"
    }
  }
}
```

### 消息节点
```json
{
  "name": "Message", 
  "data": {
    "args": {
      "name": "Host",
      "content": "你好",
      "role": "user"
    }
  }
}
```

## 🎯 推荐方案

**对于新用户**：建议使用**方案1 (Gradio界面)**
- 提供可视化界面
- 易于使用和测试
- 支持实时交互

**对于有编程经验的用户**：建议使用**方案3 (直接编程)**
- 最大灵活性
- 易于版本控制
- 便于集成到现有项目

## ⚠️ 注意事项

1. **API密钥**：在所有示例中都需要设置实际的API密钥
2. **模型配置**：确保模型配置正确匹配您的API提供商
3. **版本兼容性**：随着AgentScope的更新，配置格式可能会变化

## 🔮 未来展望

根据官方文档，"新的AgentScope低代码开发平台即将上线"，建议关注官方更新获取最新的拖拽式开发工具。

---

虽然当前版本的Workstation不可用，但AgentScope仍然提供了强大的多智能体开发能力！ 